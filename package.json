{
  "name": "chordpath",
  "version": "3.0.0",
  "description": "Single-file HTML piano sight-reading trainer",
  "private": true,
  "scripts": {
    "serve": "http-server -p 8080 -c-1 -s .",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "http-server": "^14.1.1"
  }
}
